#!/usr/bin/env bash

source "$UTIL_SCRIPT_DIR/check_installed.sh"

check_installed "awk"

TEMP=$(cat /sys/class/thermal/thermal_zone0/temp)

printf "Temp: %.2f\n" "$(awk -v temp="$TEMP" 'BEGIN { printf "%.2f", temp / 1000 }')"
